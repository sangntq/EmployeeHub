# =============================================
# EmployeeHub — Staging 環境変数テンプレート
#
# 使用方法:
#   cp .env.staging.example .env.staging
#   # 各値を AWS リソースの実際の値に書き換える
# =============================================

# --- 環境識別 ---
ENVIRONMENT=staging

# --- アプリケーション ---
# openssl rand -hex 32 で生成すること
SECRET_KEY=<replace-with-strong-random-secret>

# --- データベース（AWS RDS PostgreSQL）---
DB_USER=employeehub
DB_PASSWORD=<replace-with-rds-password>
DB_NAME=employeehub_staging
# RDS エンドポイントに置き換える
DATABASE_URL=postgresql+asyncpg://employeehub:<password>@<rds-endpoint>:5432/employeehub_staging
DATABASE_URL_SYNC=postgresql://employeehub:<password>@<rds-endpoint>:5432/employeehub_staging

# --- CORS（フロントエンドの ALB ドメイン）---
CORS_ORIGINS=https://staging.employeehub.example.com

# --- Google OAuth ---
GOOGLE_CLIENT_ID=<replace-with-google-client-id>
GOOGLE_CLIENT_SECRET=<replace-with-google-client-secret>

# --- ファイルストレージ（AWS S3）---
# S3_ENDPOINT_URL は空にする（AWS S3 標準エンドポイントを使用）
S3_ENDPOINT_URL=
S3_ACCESS_KEY=<replace-with-iam-access-key>
S3_SECRET_KEY=<replace-with-iam-secret-key>
S3_BUCKET_NAME=employeehub-staging

# --- メール送信（AWS SES または SMTP）---
SMTP_HOST=email-smtp.ap-northeast-1.amazonaws.com
SMTP_PORT=587
SMTP_USER=<replace-with-ses-smtp-user>
SMTP_PASSWORD=<replace-with-ses-smtp-password>

# --- AI 検索 ---
ANTHROPIC_API_KEY=<replace-with-anthropic-api-key>
